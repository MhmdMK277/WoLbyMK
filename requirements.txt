# Core app has NO required dependencies (stdlib only: tkinter, socket, json).

# Optional: modern UI theme (app falls back to styled tkinter without it)
customtkinter>=5.2.0

# Build-time only: .exe packaging
pyinstaller>=6.0
